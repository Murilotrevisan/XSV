# Protocol documentation

This directory explains communication behavior, rationale, compatibility, and
message-flow diagrams once defined. No transport or encoding is selected.

Authoritative machine-readable contracts belong in
[`protocol/definitions/`](../../protocol/definitions/). Link to those definitions
instead of maintaining independent copies of message fields or numeric values.
Implementation-specific encoding and transport code stays with its application.
